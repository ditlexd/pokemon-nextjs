# Shortcut Next.js demo

To run:

- If you don't have nodejs installed, install it.
- Clone the repo
- Install dependencies

```bash
npm install
```

Run the project, it is available on port 3000

```bash
npm run dev
```
